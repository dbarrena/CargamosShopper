<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="custom_toolbar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:fontFamily">@font/gotham_rounded_book</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="switch_available" parent="Base.TextAppearance.AppCompat.Widget.Switch">
        <item name="android:fontFamily">@font/gotham_rounded_book</item>
        <item name="android:switchPadding">4dp</item>
        <item name="android:switchMinWidth">60dp</item>
        <item name="android:textColor">@color/gray</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:theme">@style/switch_available_toggle</item>
    </style>

    <style name="switch_available_toggle" parent="Theme.AppCompat">
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorSwitchThumbNormal">@color/light_gray</item>
        <item name="android:colorForeground">@color/colorAccent</item>
    </style>

    <style name="TextAppearanceCustomFont">
        <item name="android:fontFamily">@font/gotham_rounded_medium</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="SliderTextFoodOrders">
        <item name="android:fontFamily">@font/gotham_rounded_medium</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="TabStyle" parent="TextAppearance.Design.Tab">
        <item name="android:fontFamily">@font/gotham_rounded_medium</item>
        <item name="android:textSize">18sp</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="custom_edittext" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlActivated">@color/colorPrimaryText</item>
        <item name="colorControlHighlight">@color/colorPrimaryText</item>
    </style>

</resources>
